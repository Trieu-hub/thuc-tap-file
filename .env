# Demo-only credentials for the local sandbox. Committed on purpose so `docker compose up --build` works in one step.
MYSQL_ROOT_PASSWORD=root_demo_pwd
RABBITMQ_USER=sandbox
RABBITMQ_PASSWORD=sandbox_demo_pwd
