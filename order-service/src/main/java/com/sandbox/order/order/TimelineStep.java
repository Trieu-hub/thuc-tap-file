package com.sandbox.order.order;

/** Values of {@code order_timeline.step}; unique per order (uk_order_timeline_order_step). */
public enum TimelineStep {

	PAYMENT, POLICY_ISSUANCE, PROCESSING_FAILED

}
